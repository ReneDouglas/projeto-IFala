package br.edu.ifpi.ifala.notificacao.enums;

/**
 * Representa os tipos de notificação disponíveis no sistema IFala.
 *
 * - INTERNO: notificações exibidas na interface do sistema (dashboard) - EXTERNO: notificações
 * enviadas por e-mail aos usuários
 */
public enum TipoNotificacao {
    INTERNO, EXTERNO
}
