package br.edu.ifpi.ifala.notificacao.enums;

/**
 * Define os tipos de notificações disponíveis no sistema IFala.
 *
 * - INTERNO: notificações exibidas dentro do dashboard (interface do sistema)
 * - EXTERNO: notificações enviadas por e-mail aos usuários
 */
public enum TiposNotificacao {
    INTERNO,
    EXTERNO
}
